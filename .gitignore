# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env*.local
.env.production
.env.development
.env.staging
.env.test

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase
.supabase/
supabase/.temp/

# 영상 파일 — 배포 시 CDN/Vercel Storage에 별도 업로드
public/videos/*.mp4
public/videos/*.mov
public/videos/*.avi

# 공공데이터 한식 raw 자료 (한식진흥원 등) — 라이선스 / 용량 이유로 git 미포함.
# 매칭 스크립트가 lib/data/hansik-enriched.json 결과만 산출 → 그것만 커밋.
/data/hansik-raw/

# AI 생성 음식 이미지 raw — Supabase Storage 업로드 후 URL 만 보존
# (Vercel 번들 크기 절감 — public/ 에 두지 않고 별도 디렉토리)
/data/ai-images/
